---
vasm:
  alias: self-eval-deprecated-format-doc-compat
  intent: "Verify deprecated doc format remains compatible but reports a deprecation diagnostic."
  compile:
    format: doc
    targetLangs: ["en"]
---

# Deprecated Format Compatibility

This document intentionally uses the deprecated `doc` format for compatibility testing.
