includes:
  - "docs-src/*.lync.md"
  - "skill-src/lync-expert/lync-expert.lync.md"

output:
  dir: "./dist"

baseDir: "./"

# 交叉编译目标语种（按输出格式分别配置）
compile:
  doc:
    targetLangs:
      - "zh-CN"
  exec:
    targetLangs:
      - "zh-CN"

routing:
  # 文档源文件直接路由到项目根目录
  - match: "docs-src/*.lync.md"
    dest: "./"

  # extract-lync-knowledge 是临时 Prompt 文件，产物输出到 .lync/（gitignore）
  - match: "skill-src/lync-expert/extract-lync-knowledge.lync.md"
    dest: "./.lync/"

  # lync-expert 技能源文件路由到 skills 目录
  - match: "skill-src/lync-expert/*.lync.md"
    dest: "./skills/lync-expert/"
